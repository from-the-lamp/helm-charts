apiVersion: v2
name: lamp-postgres-cluster
version: 0.3.0

annotations:
  notes: "PostgreSQL cluster support via CloudNativePG, core update and optimizations."

dependencies:
  - name: cluster
    version: 0.8.1
    repository: https://cloudnative-pg.github.io/charts