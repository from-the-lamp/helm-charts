{{- if .Values.dashboards.argocd.enabled }}
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: argocd
spec:
  folder: General
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  grafanaCom:
    id: 14584
    revision: 1
{{- end }}
