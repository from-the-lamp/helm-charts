apiVersion: v2
name: lamp-loki
version: 0.3.0

annotations:
  notes: "Update rollout-operator to v0.33.0, default webhooks enabled."

dependencies:
  - name: loki
    version: 6.55.0
    repository: https://grafana.github.io/helm-charts