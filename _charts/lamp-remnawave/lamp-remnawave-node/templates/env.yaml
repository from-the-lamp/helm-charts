apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "remnawave-node.fullname" . }}-config
data:
  APP_PORT: "2222"
{{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
