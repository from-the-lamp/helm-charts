apiVersion: v2
name: lamp-oauth2-proxy
version: 1.2.0

dependencies:
  - name: oauth2-proxy
    version: 10.7.0
    repository: https://oauth2-proxy.github.io/manifests

annotations:
  notes: "OAuth2 Proxy wrapper chart; supports standard authentication and ingress configuration."
