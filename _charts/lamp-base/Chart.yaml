apiVersion: v2
name: lamp-base
description: Base platform chart with shared defaults for standardized application deployments
version: 0.0.4

annotations:
  notes: "Base platform components and shared defaults for standardized deployments."
