remoteSecretKeys:
  tailscale: tailscale

tailscale-operator:
  operatorConfig:
    # defaultTags: {}
    #   - "tag:k8s-operator"
  proxyConfig: {}
    # defaultTags: "tag:k8s"

externalSecrets:
  kind: ClusterSecretStore
  name: oracle
  refreshInterval: 1h
  creationPolicy: Orphan
