{{- if index .Values "analysis" "http-200" "enabled" }}
apiVersion: argoproj.io/v1alpha1
kind: ClusterAnalysisTemplate
metadata:
  name: http-200
spec:
  args:
    - name: url
  metrics:
    - name: http-ok
      count: 3
      interval: 3s
      successCondition: result == 200
      failureCondition: result != 200
      provider:
        web:
          url: "{{`{{args.url}}`}}"
          method: GET
          timeoutSeconds: 5
{{- end }}
